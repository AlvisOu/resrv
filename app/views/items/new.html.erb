<div class="item-shell">
  <div class="item-card">
    <h1 class="item-card-title">Add a new item</h1>
    <p class="item-card-subtitle">
      Add inventory and availability for <strong><%= @workspace.name %></strong>.
    </p>

    <%= render 'form' %>
  </div>
</div>

